<apex:page
  showHeader="false"
  sidebar="false"
  standardStylesheets="false"
  applyHtmlTag="false"
  docType="html-5.0"
  contentType="text/html"
>
  <html lang="ja">
    <head>
      <meta charset="UTF-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0" />
      <style>
        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }
        html,
        body {
          width: 100%;
          height: 100%;
          overflow: hidden;
          background: #1a1a2e;
        }
        #slide-container {
          width: 100%;
          height: 100%;
          position: relative;
        }
        section {
          display: none !important;
          width: 100% !important;
          height: 100% !important;
          position: absolute !important;
          top: 0;
          left: 0;
          overflow: hidden;
        }
        section.marp-visible {
          visibility: visible !important;
          z-index: 1 !important;
        }
      </style>
    </head>
    <body>
      <div id="slide-container"></div>

      <script src="{!URLFOR($Resource.marpCore)}"></script>
      <script>
        (function () {
          var slideCount = 0;
          var currentSlide = 0;
          var container = document.getElementById("slide-container");

          function showSlide(index) {
            var sections = container.querySelectorAll("section");
            sections.forEach(function (s, i) {
              s.classList.toggle("marp-visible", i === index);
            });
            currentSlide = index;
            sendMessage({
              type: "SLIDE_CHANGED",
              slide: currentSlide,
              slideCount: slideCount
            });
          }

          function renderMarkdown(markdown) {
            try {
              var marp = new MarpCore.Marp({ html: false });
              var result = marp.render(markdown);

              var existing = document.getElementById("marp-style");
              if (existing) existing.remove();
              var style = document.createElement("style");
              style.id = "marp-style";
              style.textContent =
                result.css +
                "\nsection{visibility:hidden!important;position:absolute!important;top:0!important;left:0!important;width:100%!important;height:100%!important;z-index:0}" +
                "\nsection.marp-visible{visibility:visible!important;z-index:1!important}";
              document.head.appendChild(style);

              container.innerHTML = result.html;
              slideCount = container.querySelectorAll("section").length;
              currentSlide = 0;
              showSlide(0);
              sendMessage({ type: "READY", slideCount: slideCount });
            } catch (e) {
              sendMessage({ type: "ERROR", message: String(e) });
            }
          }

          function sendMessage(data) {
            window.parent.postMessage(data, "*");
          }

          window.addEventListener("message", function (event) {
            var data = event.data;
            if (!data || !data.type) return;
            if (data.type === "RENDER") {
              renderMarkdown(data.markdown);
            } else if (data.type === "GOTO") {
              var idx = Math.max(0, Math.min(data.slide, slideCount - 1));
              showSlide(idx);
            } else if (data.type === "PREV") {
              if (currentSlide > 0) showSlide(currentSlide - 1);
            } else if (data.type === "NEXT") {
              if (currentSlide < slideCount - 1) showSlide(currentSlide + 1);
            } else if (data.type === "PING") {
              sendMessage({ type: "PONG" });
            }
          });

          window.addEventListener("load", function () {
            sendMessage({ type: "PAGE_READY" });
          });
        })();
      </script>
    </body>
  </html>
</apex:page>
