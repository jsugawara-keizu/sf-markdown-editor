# バックログ

`sf-markdown-editor` 本体・関連コンポーネントに対するユーザーフィードバックや機能拡張アイデアを記録する。まだ `hk.issue`（課題管理）への起票は行っていないローカルメモ。着手する際に `hk.issue` リポジトリの `docs/task/` へ課題番号（T-XXXXXX）付きで正式に移行する。

## ファイル一覧

| ファイル                                   | 概要                                                                                     | 状態                                                              |
| ------------------------------------------ | ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| [10_user-feedback.md](10_user-feedback.md) | ユーザーフィードバック・機能拡張アイデア（UI列分け・ToDoアラーム・複数Markdown・画像等） | 🟡 一部対応（#1・#2 は Task 作成時のみ対応済み。#3・#4 は検討中） |

> ファイル番号は `sf-gantt-lwc` の `docs/backlog/` の採番を引き継いでいる（元は同リポジトリの `10_user-feedback.md` として作成され、2026-08-19 に本リポジトリへ移動した）。
